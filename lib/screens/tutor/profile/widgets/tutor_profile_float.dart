import 'package:flutter/material.dart';

class TutorProfileFloat extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
          return FloatingActionButton(
              child: Icon(Icons.add),
              tooltip: 'Add a new qualification',
              onPressed: () {},
              );
  }
}
